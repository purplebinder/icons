require 'spec_helper'

describe Purplebinder::Icons do
  it 'has a version number' do
    expect(Purplebinder::Icons::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
